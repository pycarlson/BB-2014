<h2>The @family.code Family</h2>
    <%= link_to "Adopt this Family!", new_donor_path(@donor, :family_id => @family.id) %>

<br><br>
<div class="col-sm-12 adoptable-families">


    <% @family.family_members.each do |fm| %>
    <div class="col-sm-4 family-members">
    <li>
        
            <ul>
              <li><%= fm.first_name %></li>
              <li><%= fm.bio %></li>
            </ul>
          

          <br>
      </li>
      </div>
      <% end %>

  </div>

