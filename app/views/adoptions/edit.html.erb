<%= render 'shared/sub_nav' %>
<div class="confirming-adoption col-lg-12">

<h1>Edit adoption</h1>

<%= render 'form' %>

<%= link_to 'Back', user_path(User.find(@adoption.user_id)), class: 'btn btn-medium btn-danger' %>

</div>
